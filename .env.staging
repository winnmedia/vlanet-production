# VLANET 스테이징 환경 변수 설정
# 이 파일은 스테이징 배포 시 참조용입니다
# 실제 값은 Vercel 대시보드에서 설정해야 합니다

# ===========================================
# Supabase 설정 (스테이징용)
# ===========================================
# Supabase 프로젝트 URL (스테이징용)
NEXT_PUBLIC_SUPABASE_URL=https://your-staging-project.supabase.co

# Supabase Anon Public Key (스테이징용)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-staging-anon-key

# Supabase Service Role Key (스테이징용 - 민감한 키)
SUPABASE_SERVICE_ROLE_KEY=your-staging-service-role-key

# ===========================================
# 애플리케이션 설정
# ===========================================
# 스테이징 애플리케이션 URL
NEXT_PUBLIC_APP_URL=https://vlanet-staging.vercel.app

# 환경 구분
NODE_ENV=development

# ===========================================
# 디버깅 및 개발 설정
# ===========================================
# 상세 로깅 활성화
DEBUG=true

# Next.js 개발 모드 설정
NEXT_TELEMETRY_DISABLED=1

# React 개발 도구 활성화
REACT_EDITOR=vscode

# ===========================================
# 테스트 및 모니터링 (스테이징용)
# ===========================================
# Sentry DSN (스테이징 전용)
NEXT_PUBLIC_SENTRY_DSN=https://your-staging-sentry-dsn.ingest.sentry.io/project-id

# 스테이징 환경 식별자
NEXT_PUBLIC_ENVIRONMENT=staging

# ===========================================
# 성능 및 제한 설정 (관대한 설정)
# ===========================================
# 최대 파일 크기 (스테이징: 100MB)
MAX_FILE_SIZE=104857600

# 허용된 비디오 형식 (스테이징: 더 다양한 형식 허용)
ALLOWED_VIDEO_FORMATS=mp4,webm,mov

# ===========================================
# 데이터베이스 연결 설정
# ===========================================
# 스테이징 연결 제한 (더 여유롭게)
DB_POOL_MIN=1
DB_POOL_MAX=5

# ===========================================
# 캐시 설정 (스테이징: 짧은 캐시)
# ===========================================
# CDN 캐시 시간 (스테이징: 1시간)
STATIC_CACHE_MAX_AGE=3600

# API 캐시 시간 (스테이징: 1분)
API_CACHE_MAX_AGE=60

# ===========================================
# 스테이징 전용 기능 플래그
# ===========================================
# 개발 도구 활성화
ENABLE_DEV_TOOLS=true

# API 요청 로깅
LOG_API_REQUESTS=true

# 성능 분석 도구
ENABLE_PERFORMANCE_PROFILING=true

# ===========================================
# Vercel 스테이징 설정
# ===========================================
# Vercel에서 자동으로 설정되는 환경 변수들
# VERCEL=1
# VERCEL_ENV=preview
# VERCEL_URL=vlanet-staging.vercel.app
# VERCEL_REGION=icn1
# VERCEL_GIT_COMMIT_SHA=
# VERCEL_GIT_COMMIT_MESSAGE=
# VERCEL_GIT_COMMIT_REF=