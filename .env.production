# VLANET 프로덕션 환경 변수 설정
# 이 파일은 프로덕션 배포 시 참조용입니다
# 실제 값은 Vercel 대시보드에서 설정해야 합니다

# ===========================================
# Supabase 설정 (필수)
# ===========================================
# Supabase 프로젝트 URL (프로덕션용)
NEXT_PUBLIC_SUPABASE_URL=https://your-production-project.supabase.co

# Supabase Anon Public Key (프로덕션용)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-production-anon-key

# Supabase Service Role Key (프로덕션용 - 민감한 키)
SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key

# ===========================================
# 애플리케이션 설정
# ===========================================
# 프로덕션 애플리케이션 URL
NEXT_PUBLIC_APP_URL=https://vlanet.vercel.app

# 환경 구분
NODE_ENV=production

# ===========================================
# 모니터링 및 분석 (선택적)
# ===========================================
# Vercel Analytics (자동으로 설정됨)
VERCEL_ANALYTICS_ID=

# Google Analytics 측정 ID (선택)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry DSN (에러 추적, 강력 권장)
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn.ingest.sentry.io/project-id

# ===========================================
# 보안 설정
# ===========================================
# JWT Secret (추후 구현시)
JWT_SECRET=your-super-secure-jwt-secret-min-32-chars

# Webhook Secret (Supabase → Vercel 통신용)
SUPABASE_WEBHOOK_SECRET=your-webhook-secret

# ===========================================
# 파일 업로드 제한 설정
# ===========================================
# 최대 파일 크기 (200MB)
MAX_FILE_SIZE=209715200

# 허용된 비디오 형식
ALLOWED_VIDEO_FORMATS=mp4

# ===========================================
# 성능 설정
# ===========================================
# CDN 캐시 시간 (초)
STATIC_CACHE_MAX_AGE=31536000

# API 캐시 시간 (초)
API_CACHE_MAX_AGE=300

# ===========================================
# 데이터베이스 연결 풀 설정
# ===========================================
# Supabase 연결 제한
DB_POOL_MIN=2
DB_POOL_MAX=10

# ===========================================
# 배포 정보 (자동 설정)
# ===========================================
# Vercel에서 자동으로 설정되는 환경 변수들
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=vlanet.vercel.app
# VERCEL_REGION=icn1
# VERCEL_GIT_COMMIT_SHA=
# VERCEL_GIT_COMMIT_MESSAGE=